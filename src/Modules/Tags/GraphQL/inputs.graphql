input CreateTagInput {
    "Name of the tag"
    tag_name: String! @rules(apply: ["required", "string", "max:255"])
}

input UpdateTagInput {
    "Name of the tag"
    tag_name: String @rules(apply: ["sometimes", "string", "max:255"])
}
